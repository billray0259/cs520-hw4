package controller;

import model.BlockIndex;
import model.Player;
import model.RowGameModel;

public class HumanPlayer implements PlayerStrategy {
    // Implementation for human player's getNextMove method
    public BlockIndex getNextMove(RowGameModel game, Player player) {
        // Get user input for the row and column of their next move
        return null;
    }
}
