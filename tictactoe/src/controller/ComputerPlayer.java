package controller;

import model.BlockIndex;
import model.Player;
import model.RowGameModel;



public class ComputerPlayer implements PlayerStrategy {
    // Implementation for computer player's getNextMove method
    public BlockIndex getNextMove(RowGameModel game, Player player) {
        // Get the next move for the computer player
        return null;
    }
    
}
